# Copy to .env (gitignored) and put your own key in.
# The theme console's proxy reads this; it never reaches the browser and never
# enters the repo. The published site does not use it at all — it falls back to
# the local derivation, which needs no key.
ANTHROPIC_API_KEY=sk-ant-...
